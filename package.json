{
  "name": "jeroboam-120",
  "version": "1.0.0",
  "private": true,
  "description": "Jéroboam 120 — application de gestion de restaurant (Android + iOS via Capacitor)",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "build": "npm run sync:web && cap sync",
    "cap:sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "sharp": "^0.35.3"
  }
}
